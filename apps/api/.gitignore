# Dependencies
node_modules/

# Build output
dist/
tsconfig.tsbuildinfo

# Test coverage
coverage/

# Environment
.env
.env.local
.env.*.local

# NOTE: never ignore ./test here - it holds the jest e2e suite that IS
# tracked in git (`pnpm --filter api test:e2e`). A previous copy-pasted
# starter gitignore contained `/test`, which silently untracked the whole
# suite from any fresh commit.
